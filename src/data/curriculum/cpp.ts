import { Lesson } from '../../types';

export const cppLessons: Lesson[] = [
  {
    id: 'cpp-01',
    number: 1,
    title: 'Hello, C++ & iostream',
    description: 'Learn preprocessor directives, namespaces, standard output stream, and semicolons.',
    language: 'cpp',
    code: `#include <iostream>\nusing namespace std;\n\nint main() {\n\tcout << "Hello, C++!" << endl;\n\treturn 0;\n}`,
    keyFocus: ['#', '<', '>', ';', '{', '}', '<', '<'],
    explanation: [
      {
        heading: 'Header Includes (#include <iostream>)',
        body: '`#include` directs the preprocessor to load the standard input/output stream library header. Type angle brackets `<` and `>` with Right Middle and Right Ring while holding Left Shift.',
        lineRange: [1, 1],
      },
      {
        heading: 'Namespaces & Semicolons (;)',
        body: '`using namespace std;` brings standard identifiers into global scope. Every statement terminates with a semicolon `;`. Strike `;` with your Right Pinky without shift.',
        lineRange: [2, 2],
      },
      {
        heading: 'The main() Entrypoint & Stream Insertion (<<)',
        body: 'Program execution originates in `int main()`. `cout <<` streams characters to standard output. Double-tap `<` with Right Middle + Left Shift in quick succession.',
        lineRange: [4, 7],
      },
    ],
    expectedOutput: `Hello, C++!`,
  },
  {
    id: 'cpp-02',
    number: 2,
    title: 'Variables & Data Types',
    description: 'Practice static typing, assignment, stream chaining, and precision.',
    language: 'cpp',
    code: `#include <iostream>\nusing namespace std;\n\nint main() {\n\tint level = 5;\n\tdouble speed = 9.81;\n\tchar grade = 'A';\n\tcout << "Lvl: " << level << ", Spd: " << speed << endl;\n\treturn 0;\n}`,
    keyFocus: ['=', ';', '<', '>', "'", '"', '.'],
    explanation: [
      {
        heading: 'Strong Static Typing',
        body: 'C++ mandates explicit types before variable names: `int` for whole numbers, `double` for decimals, and `char` for single bytes.',
        lineRange: [5, 7],
      },
      {
        heading: 'Character vs String Literals',
        body: "Single characters use single quotes 'A' (Right Pinky), whereas string literals use double quotes \"...\". Practice alternating without hesitation.",
        lineRange: [7, 7],
      },
      {
        heading: 'Stream Operator Chaining',
        body: 'You can chain multiple expressions into `cout` using repeated `<<` stream insertion operators, ending with `endl` to flush the buffer.',
        lineRange: [8, 8],
      },
    ],
    expectedOutput: `Lvl: 5, Spd: 9.81`,
  },
  {
    id: 'cpp-03',
    number: 3,
    title: 'Conditionals & Code Braces',
    description: 'Structure decisions using if-else logic and curly braces.',
    language: 'cpp',
    code: `#include <iostream>\nusing namespace std;\n\nint main() {\n\tint score = 85;\n\tif (score >= 75) {\n\t\tcout << "Passed!" << endl;\n\t} else {\n\t\tcout << "Try again." << endl;\n\t}\n\treturn 0;\n}`,
    keyFocus: ['(', ')', '{', '}', '>', '=', ';'],
    explanation: [
      {
        heading: 'Parenthesized Conditions',
        body: 'Conditions in C++ must be enclosed in parentheses `(...)`. Practice fluid Right Ring `(` and Right Pinky `)`.',
        lineRange: [6, 6],
      },
      {
        heading: 'Block Braces ({})',
        body: 'Compound statements are enclosed within curly braces `{` and `}`. Always use Right Pinky + Left Shift.',
        lineRange: [6, 10],
      },
      {
        heading: 'Else Clause & Branching',
        body: 'The `else` branch executes whenever the `if` test evaluates to false, controlling divergent execution paths.',
        lineRange: [8, 10],
      },
    ],
    expectedOutput: `Passed!`,
  },
  {
    id: 'cpp-04',
    number: 4,
    title: 'For Loops & Arrays',
    description: 'Type traditional C-style for loops with increment operators and array indexing.',
    language: 'cpp',
    code: `#include <iostream>\nusing namespace std;\n\nint main() {\n\tint nums[3] = {10, 20, 30};\n\tfor (int i = 0; i < 3; i++) {\n\t\tcout << "Item: " << nums[i] << endl;\n\t}\n\treturn 0;\n}`,
    keyFocus: ['[', ']', '{', '}', ';', '<', '+', '+'],
    explanation: [
      {
        heading: 'Fixed-Size Arrays ([])',
        body: 'Arrays declare a fixed capacity inside `[3]`. Initializer lists use braces `{10, 20, 30}`.',
        lineRange: [5, 5],
      },
      {
        heading: 'The 3-Part For Loop',
        body: 'The `for` header has initialization `int i = 0`, condition `i < 3`, and post-increment `i++`, delimited by semicolons `;`.',
        lineRange: [6, 6],
      },
      {
        heading: 'Subscript Indexing (nums[i])',
        body: 'Array elements are accessed by zero-based index using square brackets `[i]`. Reach for `[` and `]` with Right Pinky.',
        lineRange: [7, 7],
      },
    ],
    expectedOutput: `Item: 10\nItem: 20\nItem: 30`,
  },
  {
    id: 'cpp-05',
    number: 5,
    title: 'Functions & Return Types',
    description: 'Define functions with explicit return types, parameter lists, and return statements.',
    language: 'cpp',
    code: `#include <iostream>\nusing namespace std;\n\nint multiply(int a, int b) {\n\treturn a * b;\n}\n\nint main() {\n\tint product = multiply(6, 7);\n\tcout << "Product: " << product << endl;\n\treturn 0;\n}`,
    keyFocus: ['(', ')', ',', '*', '{', '}', ';'],
    explanation: [
      {
        heading: 'Function Signature & Return Types',
        body: 'The return type `int` comes first, followed by function identifier `multiply` and typed parameters `(int a, int b)`.',
        lineRange: [4, 4],
      },
      {
        heading: 'The Multiplication Operator (*)',
        body: 'The asterisk `*` performs arithmetic multiplication. Type `*` using your Right Middle finger while holding Left Shift.',
        lineRange: [5, 5],
      },
      {
        heading: 'Calling & Binding Results',
        body: '`multiply(6, 7)` executes the function logic and binds its returned integer to `product` before streaming to `cout`.',
        lineRange: [9, 10],
      },
    ],
    expectedOutput: `Product: 42`,
  },
  {
    id: 'cpp-06',
    number: 6,
    title: 'Pass by Reference (&) & Mutability',
    description: 'Mutate caller variables directly without memory duplication using references.',
    language: 'cpp',
    code: `#include <iostream>\nusing namespace std;\n\nvoid addBonus(int &score, int bonus) {\n\tscore += bonus;\n}\n\nint main() {\n\tint userScore = 75;\n\taddBonus(userScore, 25);\n\tcout << "Final Score: " << userScore << endl;\n\treturn 0;\n}`,
    keyFocus: ['&', '+', '=', ';', '(', ')', '{', '}'],
    explanation: [
      {
        heading: 'Pass-by-Reference with Ampersand (&)',
        body: 'Appending `&` to a parameter type creates an alias to the original variable rather than a copy. Strike `&` with Right Index + Left Shift.',
        lineRange: [4, 4],
      },
      {
        heading: 'In-Place Mutation (+=)',
        body: '`score += bonus;` directly mutates the caller memory address. Practice a smooth two-character transition for `+=`.',
        lineRange: [5, 5],
      },
      {
        heading: 'Zero Memory Copy Overhead',
        body: 'Passing large structures or objects by reference eliminates performance bottlenecks caused by stack copy operations.',
        lineRange: [4, 6],
      },
      {
        heading: 'Observing Caller State Changes',
        body: 'Notice how `userScore` starts at 75, passes into `addBonus`, and successfully reflects the mutated value 100 in `main()`.',
        lineRange: [9, 11],
      },
    ],
    expectedOutput: `Final Score: 100`,
  },
  {
    id: 'cpp-07',
    number: 7,
    title: 'Pointers & Address-Of Operator',
    description: 'Directly manipulate memory addresses using pointers and dereferencing.',
    language: 'cpp',
    code: `#include <iostream>\nusing namespace std;\n\nint main() {\n\tint target = 42;\n\tint *ptr = &target;\n\t*ptr = 99;\n\tcout << "Direct: " << target << ", Via Pointer: " << *ptr << endl;\n\treturn 0;\n}`,
    keyFocus: ['*', '&', '=', ';', '<', '>', '{', '}'],
    explanation: [
      {
        heading: 'The Address-Of Operator (&)',
        body: '`&target` evaluates to the hexadecimal memory address where `target` resides in RAM. Type `&` with Right Index + Left Shift.',
        lineRange: [6, 6],
      },
      {
        heading: 'Pointer Declaration & Asterisks (*)',
        body: '`int *ptr` declares a variable that stores a memory address. Strike `*` with your Right Middle finger while holding Left Shift.',
        lineRange: [6, 6],
      },
      {
        heading: 'Dereferencing (*ptr = 99)',
        body: 'Applying `*` to an existing pointer accesses the value at that address. Writing `*ptr = 99;` directly overwrites `target`.',
        lineRange: [7, 7],
      },
      {
        heading: 'Memory Address Aliasing',
        body: 'Both `target` and `*ptr` refer to the exact same cell in memory, so printing either yields 99.',
        lineRange: [8, 8],
      },
    ],
    expectedOutput: `Direct: 99, Via Pointer: 99`,
  },
  {
    id: 'cpp-08',
    number: 8,
    title: 'Structs & Member Dot Operator (.)',
    description: 'Group composite records and access members using the dot selection operator.',
    language: 'cpp',
    code: `#include <iostream>\n#include <string>\nusing namespace std;\n\nstruct Hero {\n\tstring name;\n\tint health;\n\tint power;\n};\n\nint main() {\n\tHero h = {"Aria", 100, 45};\n\tcout << "Hero: " << h.name << " | HP: " << h.health << endl;\n\treturn 0;\n}`,
    keyFocus: ['.', ';', '{', '}', '"', '<', '>', '#'],
    explanation: [
      {
        heading: 'Struct Declaration & Semicolon (;)',
        body: 'Structs group related variables into a single record. Remember that struct declarations MUST end with a closing brace and semicolon `};`.',
        lineRange: [5, 9],
      },
      {
        heading: 'Aggregate Initializer Lists ({})',
        body: '`Hero h = {"Aria", 100, 45};` initializes members in declaration order using clean curly braces `{}`.',
        lineRange: [12, 12],
      },
      {
        heading: 'Member Selection Operator (.)',
        body: 'Access internal struct fields using the period `.`. Reach down to `.` with your Right Ring finger.',
        lineRange: [13, 13],
      },
      {
        heading: 'The <string> Header',
        body: 'Standard C++ strings require `#include <string>` for dynamic text storage and seamless stream insertion.',
        lineRange: [2, 2],
      },
    ],
    expectedOutput: `Hero: Aria | HP: 100`,
  },
  {
    id: 'cpp-09',
    number: 9,
    title: 'Dynamic Memory (new & delete)',
    description: 'Allocate and free heap memory safely with new, delete, and nullptr.',
    language: 'cpp',
    code: `#include <iostream>\nusing namespace std;\n\nint main() {\n\tint *data = new int(256);\n\tcout << "Allocated: " << *data << endl;\n\tdelete data;\n\tdata = nullptr;\n\tcout << "Cleaned up safely!" << endl;\n\treturn 0;\n}`,
    keyFocus: ['*', '=', ';', '(', ')', '<', '>'],
    explanation: [
      {
        heading: 'Heap Allocation with new',
        body: '`new int(256)` allocates space on the heap and returns its pointer. Heap objects survive beyond local stack frame scopes.',
        lineRange: [5, 5],
      },
      {
        heading: 'Releasing Memory with delete',
        body: 'Failing to call `delete data;` causes memory leaks. Always free heap resources when no longer required.',
        lineRange: [7, 7],
      },
      {
        heading: 'Null Pointer Safety (nullptr)',
        body: 'Assigning `data = nullptr;` immediately after deletion prevents accidental dangling pointer dereferencing crashes.',
        lineRange: [8, 8],
      },
      {
        heading: 'Clean Lifecycle Discipline',
        body: 'Good C++ development requires rigorous pairing of every `new` with a corresponding `delete`.',
        lineRange: [5, 9],
      },
    ],
    expectedOutput: `Allocated: 256\nCleaned up safely!`,
  },
  {
    id: 'cpp-10',
    number: 10,
    title: 'STL Vector & Dynamic Sizing',
    description: 'Harness standard template library vectors with dynamic growth and range loops.',
    language: 'cpp',
    code: `#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n\tvector<int> vals = {10, 20};\n\tvals.push_back(30);\n\tfor (int v : vals) {\n\t\tcout << v << " ";\n\t}\n\tcout << endl;\n\treturn 0;\n}`,
    keyFocus: ['<', '>', '_', '(', ')', '{', '}', ':', ';'],
    explanation: [
      {
        heading: 'The Vector Template (<int>)',
        body: '`std::vector` is a resizable array. The item type is specified within angle brackets `<int>`. Type `<` and `>` with Right Middle/Ring + Left Shift.',
        lineRange: [6, 6],
      },
      {
        heading: 'Method Calls (.push_back())',
        body: '`vals.push_back(30);` appends items to the tail of the vector, automatically reallocating buffer memory as needed.',
        lineRange: [7, 7],
      },
      {
        heading: 'Range-Based For Loops (:)',
        body: '`for (int v : vals)` iterates directly over elements. Strike the colon `:` with Right Pinky while holding Left Shift.',
        lineRange: [8, 10],
      },
      {
        heading: 'Stream Delimitation',
        body: 'Output elements separated by spaces `" "` followed by `cout << endl;` to conclude the output line.',
        lineRange: [9, 11],
      },
    ],
    expectedOutput: `10 20 30 `,
  },
  {
    id: 'cpp-11',
    number: 11,
    title: 'Classes & Encapsulation',
    description: 'Implement object-oriented classes with private members, initializers, and const methods.',
    language: 'cpp',
    code: `#include <iostream>\nusing namespace std;\n\nclass Counter {\nprivate:\n\tint count;\npublic:\n\tCounter(int start) : count(start) {}\n\tvoid increment() { count++; }\n\tint getCount() const { return count; }\n};\n\nint main() {\n\tCounter c(10);\n\tc.increment();\n\tcout << "Counter: " << c.getCount() << endl;\n\treturn 0;\n}`,
    keyFocus: [':', ';', '{', '}', '(', ')', '+', '+'],
    explanation: [
      {
        heading: 'Access Specifiers (private vs public)',
        body: '`private:` encapsulates internal state from external tampering. `public:` exposes safe methods for interaction.',
        lineRange: [5, 7],
      },
      {
        heading: 'Member Initializer Lists (:)',
        body: '`Counter(int start) : count(start) {}` initializes member variables before the constructor body executes, boosting performance.',
        lineRange: [8, 8],
      },
      {
        heading: 'Const Member Functions (const)',
        body: 'Marking a method `const` guarantees it will never modify any internal object fields. Type `const` with steady rhythm.',
        lineRange: [10, 10],
      },
      {
        heading: 'Object Instantiation & Invocation',
        body: '`Counter c(10);` instantiates the object with initial state 10, which increments to 11 via method calls.',
        lineRange: [14, 16],
      },
    ],
    expectedOutput: `Counter: 11`,
  },
  {
    id: 'cpp-12',
    number: 12,
    title: 'Arrow Operator (->) & Pointers',
    description: 'Navigate pointer-based object structures using the arrow dereference operator.',
    language: 'cpp',
    code: `#include <iostream>\nusing namespace std;\n\nstruct Point {\n\tint x;\n\tint y;\n};\n\nint main() {\n\tPoint pt = {15, 30};\n\tPoint *ptr = &pt;\n\tptr->x = 50;\n\tcout << "Point: (" << ptr->x << ", " << ptr->y << ")" << endl;\n\treturn 0;\n}`,
    keyFocus: ['-', '>', '*', '&', '.', ';', '(', ')'],
    explanation: [
      {
        heading: 'The Arrow Operator (->)',
        body: '`ptr->x` is shorthand for `(*ptr).x`. Practice the rapid two-key motion: minus `-` (Right Pinky) then greater-than `>` (Right Ring + Left Shift).',
        lineRange: [12, 13],
      },
      {
        heading: 'Pointer Assignment to Struct Address',
        body: '`Point *ptr = &pt;` stores the address of struct instance `pt`. Hit `*` with Right Middle and `&` with Right Index.',
        lineRange: [11, 11],
      },
      {
        heading: 'Direct Member Mutation via Arrow',
        body: 'Writing `ptr->x = 50;` modifies `pt.x` directly through the pointer reference.',
        lineRange: [12, 12],
      },
      {
        heading: 'Stream Output with Multiple Operators',
        body: 'Chaining string literals with coordinates requires disciplined typing across parentheses, commas, and `<<` operators.',
        lineRange: [13, 13],
      },
    ],
    expectedOutput: `Point: (50, 30)`,
  },
  {
    id: 'cpp-13',
    number: 13,
    title: 'Function Templates (<typename T>)',
    description: 'Author type-agnostic generic functions with template metaprogramming.',
    language: 'cpp',
    code: `#include <iostream>\nusing namespace std;\n\ntemplate <typename T>\nT getMaximum(T a, T b) {\n\treturn (a > b) ? a : b;\n}\n\nint main() {\n\tcout << "Max Int: " << getMaximum(10, 25) << endl;\n\tcout << "Max Double: " << getMaximum(4.5, 2.1) << endl;\n\treturn 0;\n}`,
    keyFocus: ['<', '>', '?', ':', '(', ')', ';', '{', '}'],
    explanation: [
      {
        heading: 'Template Header (template <typename T>)',
        body: '`template <typename T>` declares a placeholder type parameter `T`. Notice the angle brackets `<>` typed with Right Middle and Right Ring.',
        lineRange: [4, 4],
      },
      {
        heading: 'The Ternary Operator (?:)',
        body: '`(a > b) ? a : b` evaluates condition, returning `a` if true or `b` if false. Type `?` with Right Pinky + Left Shift.',
        lineRange: [6, 6],
      },
      {
        heading: 'Automatic Type Deduction',
        body: 'Calling `getMaximum(10, 25)` automatically deduces `T` as `int`, while `getMaximum(4.5, 2.1)` deduces `T` as `double`.',
        lineRange: [10, 11],
      },
      {
        heading: 'Compile-Time Specialization',
        body: 'The C++ compiler generates specialized machine code for each distinct type used in template invocations.',
        lineRange: [4, 7],
      },
    ],
    expectedOutput: `Max Int: 25\nMax Double: 4.5`,
  },
  {
    id: 'cpp-14',
    number: 14,
    title: 'Lambda Expressions ([]() {})',
    description: 'Write modern anonymous inline functions with lambda captures and execution.',
    language: 'cpp',
    code: `#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n\tauto square = [](int n) { return n * n; };\n\tvector<int> inputs = {2, 4, 6};\n\tfor (int val : inputs) {\n\t\tcout << square(val) << " ";\n\t}\n\tcout << endl;\n\treturn 0;\n}`,
    keyFocus: ['[', ']', '(', ')', '{', '}', '*', ';', ':'],
    explanation: [
      {
        heading: 'The Lambda Capture Clause ([])',
        body: 'Lambdas begin with capture brackets `[]`. Empty brackets `[]` signify that the lambda captures no surrounding variables from scope.',
        lineRange: [6, 6],
      },
      {
        heading: 'Parameter List & Inline Body',
        body: '`(int n) { return n * n; };` defines parameters and immediate body. Notice the terminating semicolon `;` after the closing brace `}`.',
        lineRange: [6, 6],
      },
      {
        heading: 'The auto Type Specifier',
        body: 'Because lambda types are unique compiler-generated closures, `auto` automatically deduces the proper callable type.',
        lineRange: [6, 6],
      },
      {
        heading: 'Inline Functional Execution',
        body: 'Inside the range loop, `square(val)` executes the lambda for each vector element, outputting the squared values `4 16 36`.',
        lineRange: [8, 10],
      },
    ],
    expectedOutput: `4 16 36 `,
  },
  {
    id: 'cpp-15',
    number: 15,
    title: 'Linked Nodes & Pointer Traversal',
    description: 'Construct pointer-linked nodes and traverse linear lists using while loops.',
    language: 'cpp',
    code: `#include <iostream>\nusing namespace std;\n\nstruct Node {\n\tint val;\n\tNode *next;\n};\n\nint main() {\n\tNode n2 = {20, nullptr};\n\tNode n1 = {10, &n2};\n\tNode *curr = &n1;\n\twhile (curr != nullptr) {\n\t\tcout << curr->val << " ";\n\t\tcurr = curr->next;\n\t}\n\tcout << endl;\n\treturn 0;\n}`,
    keyFocus: ['*', '-', '>', '!', '=', '&', ';', '{', '}'],
    explanation: [
      {
        heading: 'Self-Referential Node Pointers',
        body: '`Node *next;` declares a pointer pointing to another instance of the same struct type, forming the basis of linked lists and trees.',
        lineRange: [4, 7],
      },
      {
        heading: 'Chaining Nodes via Memory Addresses (&)',
        body: '`Node n1 = {10, &n2};` links `n1` to `n2` by passing the memory address `&n2`. Practice smooth transitions for `&` and `;`.',
        lineRange: [10, 11],
      },
      {
        heading: 'Traversal with While Loops (!= nullptr)',
        body: '`while (curr != nullptr)` walks sequentially through each node until reaching the sentinel end of the list.',
        lineRange: [13, 16],
      },
      {
        heading: 'Advancing the Pointer (curr = curr->next)',
        body: 'Updating `curr = curr->next;` shifts the pointer forward to the subsequent node on every iteration cycle.',
        lineRange: [15, 15],
      },
    ],
    expectedOutput: `10 20 `,
  },
];
